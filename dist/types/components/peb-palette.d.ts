import { LitElement } from 'lit';
export declare class PebPalette extends LitElement {
    open: boolean;
    static styles: import("lit").CSSResult;
    private presets;
    private _selectPreset;
    private _close;
    render(): import("lit-html").TemplateResult<1>;
}
